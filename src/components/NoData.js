import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const NoData = () => {
    return (
        <Text style={{textAlign: 'center', marginTop: 30, color: 'red'}}>Мэдээлэл олдсонгүй</Text>
    )
}

export default NoData

const styles = StyleSheet.create({})
